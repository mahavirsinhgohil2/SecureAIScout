# SecureAI Scout Pitch Script

All figures below come from `cache/last_run.json` generated by `python run_demo.py`: 7 raw findings, 6 real findings, 1 false alarm removed, 6 verified, command injection fixed on attempt 2, and risk 78 -> 1. The current cache contains 33 events.

## Three-minute version

| Time | What to click/show | Exact words to say | Fallback if it breaks |
|---|---|---|---|
| 0:00-0:20 | Start screen and `Start demo scan` | "This is SecureAI Scout reviewing one bundled ShopEasy demo. It stays local, explains each result, tests fixes in a temporary sandbox, and waits for a person before export." | Open replay mode and say the cache was generated by the same local run. |
| 0:20-0:40 | Investigate events and Found metric | "The agent found 7 areas to review. That is the raw scan count, before context-aware triage." | Show the first three investigate messages from the event trace. |
| 0:40-1:00 | Triage events | "One result is a false alarm: the table name is checked against a short list before the query runs. Six findings remain real." | Read the cache event beginning `False alarm removed:`. |
| 1:00-1:25 | Fix events and finding cards | "Each real issue gets a targeted snippet replacement. The UI shows the proposed change rather than pretending the original app was edited." | Show a saved diff from a finding card. |
| 1:25-1:55 | Command-injection card and verify trace | "The first command fix is intentionally rejected by the verifier: `syntax check passed; command safety check failed because unsafe command execution remains`. The agent retries with a safe subprocess list call." | Read the same proof from the cached verify event. |
| 1:55-2:15 | Successful verify event and metrics | "The second attempt passes: `syntax check passed; command safety check passed; sandbox rescan passed; demo tests passed`. Six of six real findings are verified, and the computed risk moves from 78 to 1." | Show the report event and proof text from replay mode. |
| 2:15-2:40 | Review cards, diffs, and proof | "An explanation may come from the language model or built-in rules, but Verified is never an AI opinion or a button state. It appears only when verifier proof exists. The false alarm stays out of this list, and command injection says Fixed on 2nd attempt." | Open the cached review screen. |
| 2:40-3:00 | Approve one fix, then show download | "A human still decides what leaves the review. Before approval there is no download; after approval the export contains only the approved patch." | Show the export diff or use the replay checklist. |

## Ninety-second lightning version

1. **0:00-0:15:** Start the scan. "SecureAI Scout stays on the bundled demo and reports 7 raw findings."
2. **0:15-0:30:** Show triage. "One allowlisted table lookup is removed as a false alarm, leaving 6 real findings."
3. **0:30-0:55:** Show command injection. "The verifier rejects the first patch because unsafe command execution remains, then the retry passes syntax, command safety, sandbox rescan, and demo tests."
4. **0:55-1:15:** Show review. "All 6 real findings have verifier proof; the computed risk is 78 -> 1."
5. **1:15-1:30:** Approve one verified fix. "Only a human approval reveals the patch download, and the export contains only approved changes."
